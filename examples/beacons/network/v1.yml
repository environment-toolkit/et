type: ipv4-network # Ipv4NetworkBeacon (namespace: network)
name: network-v1

props:
  size: large
overrides:
  environments:
    - name: dev
      props:
        size: small

resources:
  - name: fargate-cluster
    type: ecs-fargate-cluster # EcsFargateClusterBeacon (namespace: network)
    props: {} # TODO: add props
